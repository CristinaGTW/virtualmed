stages:
  - build
  - test

build:
  stage: build
  script:
    - cd android
    - sudo bundle install
    - cd ..
    - flutter build appbundle
    - cd android
    - bundle exec fastlane android test

test:
  stage: test
  script:
    - flutter test